services:
  - type: web
    name: anyfeastgym
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: VITE_SUPABASE_URL
        value: https://tcfvsbwpptrfoflyqieq.supabase.co
      - key: VITE_SUPABASE_PUBLISHABLE_KEY
        sync: false
      - key: VITE_GOOGLE_CLIENT_ID
        sync: false
      - key: NODE_ENV
        value: production

databases:
  - name: anyfeastgym-db
    databaseName: anyfeastgym
    user: anyfeastgym
